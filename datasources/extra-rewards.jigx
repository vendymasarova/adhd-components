type: datasource.sqlite
options:
  provider: DATA_PROVIDER_DYNAMIC

  entities:
    - default/extra-rewards

  query: 
    SELECT 
    id, 
    '$.image', 
    '$.name',
    '$.points',
    '$.user',
    '$.isCompleted'
    FROM [default/extra-rewards]
